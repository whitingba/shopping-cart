import React from 'react';

const index = ({ product }) => {
    return (
        <div>
            <div>
                {product.name}
            </div>
        </div>
    );
};

export default index;